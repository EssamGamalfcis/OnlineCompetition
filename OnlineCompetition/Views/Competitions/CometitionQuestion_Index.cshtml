@using AdminPanel.Models
@model List<CompetitionQuestionsVM>
@{ ViewData["Title"] = "Cometition Index";
    Layout = "~/Views/Shared/_Layout.cshtml"; }
<script src="~/lib/jquery/dist/jquery.js"></script>
<link href="~/css/fontawesome.css" rel="stylesheet" />
<script src="~/js/sweetalert2.js"></script>

<h1 style="text-align: center; margin-top: 10px; margin-bottom: 30px;">أعدادت ألاسألة</h1>

<div id='addCompetitionQuestionModal' class='modal fade' data-url='@Url.Action("AddOrEditCompetitionQuestions")?id=0'>
    <div id='addCompetitionQuestionContainer' class="container partialViewModal">
    </div>
</div>

<button id='addCompetitionQuestion' class="btn btn-primary btn-outline-primary" style="float: right; margin: 10px;">أضافة سؤال</button>
<table class="table table-striped">
    <thead>
        <tr>
            <th>أسم السؤال</th>
            <th>نوع الاجابة</th>
            <th>الترتيب</th>
            <th style="text-align:center">تعديل / حذف</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var OnlineCompetitionQuestion in Model)
        {
            <tr>
                  <td>@OnlineCompetitionQuestion.NameAR</td>
                  <td>@OnlineCompetitionQuestion.AnswerType.ToString()</td>
                  <td>@OnlineCompetitionQuestion.Sort</td>
                  <td style="text-align:center">
                      <a class="btn btn-danger" style="cursor: pointer; color: white;" competitionId="@OnlineCompetitionQuestion.Id" onclick="Delete(this)">Delete</a>
                      <a class="btn btn-primary" style="cursor: pointer; color: white;" competitionId="@OnlineCompetitionQuestion.Id" id="editCompetitionQuestions">Update</a>
                  </td>
            </tr> 
         }
            </tbody>
        </table>
        <script>
            $(document).ready(function () {
                $('#addCompetitionQuestion').click(function () {
                    var url = $('#addCompetitionQuestionModal').data('url');
                    $.get(url, function (data) {
                        $('#addCompetitionQuestionContainer').html(data);
                        $("#addCompetitionQuestionModal").addClass("in");
                        $(".modal-backdrop").show()
                        $("#addCompetitionQuestionModal").show();
                        $('#addCompetitionQuestionModal').modal('show');
                    });
                });
                $('#editCompetitionQuestions').click(function () {
                    var url = $('#addCompetitionQuestionModal').data('url');
                    url = url.replace('0', $(this).attr('competitionid'))
                    $.get(url, function (data) {
                        $('#addCompetitionQuestionContainer').html(data);
                        $("#addCompetitionQuestionModal").addClass("in");
                        $(".modal-backdrop").show()
                        $("#addCompetitionQuestionModal").show();
                        $('#addCompetitionQuestionModal').modal('show');
                    });
                });
            });
            function Cancel() {
                $("#addCompetitionQuestionModal").removeClass("in");
                $(".modal-backdrop").hide()
                $("#addCompetitionQuestionModal").hide();
            }
            function Delete(obj) {
                var objToSend = {};
                objToSend.Id = $(obj).attr('competitionId');
                Swal.fire({
                    title: 'تأكيد',
                    text: "هل انت متأكد من الحذف",
                    icon: 'warning',
                    showCancelButton: true,
                    confirmButtonColor: '#3085d6',
                    cancelButtonColor: '#d33',
                    confirmButtonText: 'Yes'
                }).then((result) => {
                    if (result.isConfirmed) {
                        $.ajax({
                            type: "POST",
                            url: "@Url.Action("DeleteCompetition")",
                            data: { obj: objToSend },
                            dataType: "text",
                            success: function (response) {
                                if ($.parseJSON(response).success == true || $.parseJSON(response).success == "true") {
                                    Swal.fire(
                                        'حذف',
                                        'تم الحذف بنجاح',
                                        'success'
                                    ).then((result) => {
                                        Cancel();
                                        location.reload();
                                    })

                                }
                                else {
                                    Swal.fire(
                                        'تنبية',
                                        'برجاء المحاولة فى وقت أخر',
                                        'warning'
                                    );
                                    $('#Loader').hide();
                                    $('#SaveBtn').show();
                                    $('#CancelBtn').show();
                                }
                            },
                            error: function (req, status, error) {
                                Swal.fire(
                                    'تنبية',
                                    'خطأ بالسيرفر',
                                    'warning'
                                );
                                $('#Loader').hide();
                                $('#SaveBtn').show();
                                $('#CancelBtn').show();
                            }
                        });
                    }
                })
            }
        </script>